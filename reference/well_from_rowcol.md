# Calculate well number from row and column

This function can be used in the `raw_map()` function from package
`platetools`.

## Usage

``` r
well_from_rowcol(row, col)
```

## Arguments

- row:

  A character vector with the row names

- col:

  A numeric vector with the column names

## Value

A character vector with the well numbers
