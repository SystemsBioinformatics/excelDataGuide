Schema = {
  guide.version | String,
  locations     | _,
  plate.format  | 96,
  template.max.version" | null,
  template.min.version  | String,
  template.name         | "competition",
  translations | _
}

